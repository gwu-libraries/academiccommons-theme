{#
/**
 * @file
 * Override of default theme implementation to display a region.
 * Maxs says: overriding this in order to add classes, IDs and footer/header tags I could really use for better CSS and JS selectors 
 * (trying to make them in the same style as used before either by D7 and/or GW since we're reusing some of that code)
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region <div>.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @see template_preprocess_region()
 *
 * @ingroup themeable
 */
#}

{% set tag = "div" %}
{% if region == "header" or region == "footer" %}
  {%set tag = region %}
{% endif %}

{% if content %}
  <{{ tag }}{{ attributes.addClass('region').addClass('region-' ~ region).setAttribute('id',region) }}>
    {{ content }}
  </{{ tag }}>
{% endif %}
